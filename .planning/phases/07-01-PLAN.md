# Plan: 07-01 Database Migration

**Goal**: Replace JSON file-based storage with a relational database using SQLAlchemy.

## Tasks
1. [x] **Setup SQLAlchemy** - Install `sqlalchemy` and `alembic` (for migrations).
2. [x] **Define Models** - Create `backend/core/db.py` and define `Garment` and `Merchant` models.
3. [x] **Seed Logic** - Create a script to migrate existing data from `backend/data/garments.json` into the new database.
4. [x] **Refactor API** - Update `backend/main.py` to use DB sessions instead of direct JSON manipulation.
5. [x] **Verify** - Ensure the catalog and merchant dashboard still work correctly with the new backend.

## Success Criteria
- [x] Backend starts without errors using SQLite.
- [x] Catalog items are fetched from the database.
- [x] Adding/Deleting garments in the merchant dashboard updates the database correctly.
