# Plan: 07-02 GPU Environment Repair & Real Inference Toggle

**Goal**: Repair the `torch` environment to enable CUDA support and install missing AI dependencies. Enable "Real Diffusion" inference if possible.

## Tasks
1. [x] **Python 3.12 Migration** - Created a new virtual environment with Python 3.12 for better CUDA library compatibility.
2. [x] **GPU-Ready Torch** - Installed `torch 2.11.0+cu128` successfully.
3. [x] **AI Dependencies** - Installed `diffusers`, `accelerate`, `transformers`, `huggingface_hub`, `omegaconf`, and `sam2`.
4. [x] **Memory Optimization** - Updated `vton_engine.py` with `float16`, `cpu_offload`, and `xformers` attention.
5. [x] **Verify Environment** - Confirm imports of all libraries in the new venv.
6. [x] **Integration Test** - Run a local inference test with `backend/core/vton_engine.py`.

## Success Criteria
- [x] `torch.cuda.is_available()` returns `True`.
- [x] `diffusers` and `sam2` can be imported without errors.
- [x] `VTONEngine` successfully loads the pipeline when `is_mock` is False.
- [x] Initial inference attempt doesn't crash with Out Of Memory (OOM) errors.

