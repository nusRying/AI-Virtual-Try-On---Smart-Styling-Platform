---
phase: 06-merchant-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/main.py, backend/data/garments.json]
autonomous: true
requirements: [PLAT-02]
---

<objective>
Enhance the backend to support merchant operations, including inventory management and performance metrics.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Initialize Metrics in Catalog</name>
  <files>backend/data/garments.json</files>
  <action>
    - Add `"try_on_count": 0` to all existing garments in the JSON file.
  </action>
</task>

<task type="auto">
  <name>Task 2: Implement Metrics Tracking</name>
  <files>backend/main.py</files>
  <action>
    - Update `POST /api/v1/try-on` logic to increment the `try_on_count` of the selected garment in `garments.json` whenever a task is successfully queued.
  </action>
</task>

<task type="auto">
  <name>Task 3: Implement Inventory Management APIs</name>
  <files>backend/main.py</files>
  <action>
    - Add `POST /api/v1/catalog` endpoint to allow uploading new garments (saves image to `static/garments` and adds entry to JSON).
    - Add `DELETE /api/v1/catalog/{garment_id}` endpoint to remove items.
  </action>
</task>
</tasks>
