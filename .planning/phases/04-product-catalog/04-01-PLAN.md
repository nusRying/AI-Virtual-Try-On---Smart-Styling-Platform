---
phase: 04-product-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/main.py, backend/data/garments.json, backend/static/garments/.keep]
autonomous: true
requirements: [STYL-01]
must_haves:
  truths:
    - "GET /api/v1/catalog returns a paginated list of garments"
    - "Searching via ?q=keyword returns filtered results"
    - "Try-on endpoint can accept a garment_id"
  artifacts:
    - path: "backend/data/garments.json"
      provides: "Catalog data source"
    - path: "backend/main.py"
      provides: "Catalog and updated try-on endpoints"
  key_links:
    - from: "POST /api/v1/try-on"
      to: "backend/data/garments.json"
      via: "ID lookup"
---

<objective>
Implement a dynamic catalog API that provides a searchable, paginated set of garments from a JSON data source, and update the try-on API to support selecting items from this catalog by ID.
</objective>

<execution_context>
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/main.py

<interfaces>
From backend/main.py (current state):
```python
@app.post("/api/v1/try-on")
async def try_on_async(
    user_image: UploadFile = File(...),
    garment_image: UploadFile = File(...)
):
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed Catalog Data & Static Storage</name>
  <files>backend/data/garments.json, backend/static/garments/.keep</files>
  <action>
    - Create `backend/data/garments.json` with at least 10 sample men's shirts.
    - Fields: `id`, `name`, `category`, `price`, `description`, `thumbnail_url`, `full_image_url`, `tags`.
    - Use internal paths for `thumbnail_url` and `full_image_url` (e.g., `/static/garments/shirt1_thumb.jpg`).
    - Create `backend/static/garments/` directory.
    - Since real images are missing, copy existing placeholders if any, or just ensure the directory exists for now.
    - Update `backend/main.py` to serve static files: `app.mount("/static", StaticFiles(directory="backend/static"), name="static")`.
  </action>
  <verify>
    <automated>ls backend/data/garments.json; ls backend/static/garments/</automated>
  </verify>
  <done>Catalog JSON exists and static file serving is enabled in FastAPI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Catalog API with Search/Pagination</name>
  <files>backend/main.py</files>
  <behavior>
    - GET /api/v1/catalog returns all garments by default (limited by 'limit' param).
    - GET /api/v1/catalog?q=white returns only garments with "white" in name or tags.
    - GET /api/v1/catalog?page=2&limit=2 returns the second page of results.
    - Response format: { "garments": [...], "total_count": N, "page": 1, "limit": 6 }
  </behavior>
  <action>
    - Add `GET /api/v1/catalog` endpoint to `backend/main.py`.
    - Load data from `backend/data/garments.json` on each request (keep it simple for now).
    - Implement case-insensitive search filtering on `name` and `tags`.
    - Implement pagination using `page` and `limit` query parameters.
  </action>
  <verify>
    <automated>curl "http://localhost:8000/api/v1/catalog?limit=2" | jq .garments | length == 2</automated>
  </verify>
  <done>Catalog API is functional with search and pagination.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Try-On API to support garment_id</name>
  <files>backend/main.py</files>
  <action>
    - Update `POST /api/v1/try-on` signature: `user_image: UploadFile = File(...)`, `garment_id: Optional[str] = Form(None)`, `garment_image: Optional[UploadFile] = File(None)`.
    - Logic:
      1. If `garment_id` is provided, find the garment in `garments.json`. Use its `full_image_url` local file path for processing.
      2. If `garment_id` is NOT provided, fall back to `garment_image` upload.
      3. If neither provided, return 400 Bad Request.
    - Note: The local path must be resolved from the static URL or stored separately in the JSON.
  </action>
  <verify>
    <automated># Manual verification or script that mocks the file and calls with ID</automated>
  </verify>
  <done>Try-on API can resolve garments by ID from the catalog.</done>
</task>

</tasks>

<verification>
Check catalog responsiveness and try-on task submission with ID.
</verification>

<success_criteria>
1. /api/v1/catalog returns correct JSON structure.
2. Search and pagination params work as expected.
3. /api/v1/try-on triggers a Celery task when passed a garment_id.
</success_criteria>

<output>
After completion, create `.planning/phases/04-product-catalog/04-01-SUMMARY.md`
</output>
