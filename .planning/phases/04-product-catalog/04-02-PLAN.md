---
phase: 04-product-catalog
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [frontend/src/components/ProductCatalog.tsx, frontend/src/components/ProductCatalog.css, frontend/src/App.tsx]
autonomous: false
requirements: [STYL-01]
must_haves:
  truths:
    - "User can see a searchable list of products"
    - "Clicking a product triggers the virtual try-on automatically"
    - "Pagination works as expected (updates results on page change)"
  artifacts:
    - path: "frontend/src/components/ProductCatalog.tsx"
      provides: "New catalog component with search and pagination"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "/api/v1/catalog"
      via: "fetch in ProductCatalog"
---

<objective>
Refactor the frontend to include a dynamic Product Catalog component with search and pagination features, and implement the auto-trigger logic for the try-on workflow upon garment selection.
</objective>

<execution_context>
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/App.tsx
@frontend/src/components/GarmentSelector.tsx

<interfaces>
From frontend/src/components/GarmentSelector.tsx (to be replaced):
```typescript
interface Garment {
  id: string;
  name: string;
  thumbnail: string;
  fullImage: string;
}
```

From backend/main.py (newly created in plan 01):
```json
{
  "garments": [
    {
      "id": "shirt-1",
      "name": "Classic White Shirt",
      "thumbnail_url": "/static/garments/shirt1_thumb.jpg",
      "full_image_url": "/static/garments/shirt1_full.jpg"
    }
  ],
  "total_count": 10,
  "page": 1,
  "limit": 6
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ProductCatalog component</name>
  <files>frontend/src/components/ProductCatalog.tsx, frontend/src/components/ProductCatalog.css</files>
  <action>
    - Rename/Refactor `GarmentSelector.tsx` to `ProductCatalog.tsx`.
    - Rename/Refactor `GarmentSelector.css` to `ProductCatalog.css`.
    - Maintain state for `garments`, `searchQuery`, `page`, and `totalPages`.
    - Fetch data from `http://localhost:8000/api/v1/catalog` with query params.
    - Implement a search input field (with debounce or immediate fetch on change).
    - Implement pagination controls (Previous/Next) that update the `page` state and refetch.
    - Style the catalog as a clean grid using Vanilla CSS.
  </action>
  <verify>
    <automated># Manual verification needed for UI; check component renders search box and pagination</automated>
  </verify>
  <done>ProductCatalog component correctly displays data from API and supports search/pagination.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Catalog with Try-On Workflow</name>
  <files>frontend/src/App.tsx</files>
  <action>
    - Replace `GarmentSelector` with `ProductCatalog` in `App.tsx`.
    - Update `handleGarmentSelect` to accept the new `Garment` interface (matching backend IDs).
    - Implement "Auto-Trigger" logic: When `handleGarmentSelect` is called AND `selectedPhoto` is present, immediately invoke `handleTryOn`.
    - Refactor `handleTryOn` to pass `garment_id` as part of the `FormData` instead of downloading/re-uploading the garment image file.
  </action>
  <verify>
    <automated># Manual verification: clicking a garment should start "Processing..." if photo is uploaded</automated>
  </verify>
  <done>Frontend try-on flow is now driven by the dynamic catalog and auto-triggers.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Visual & Functional UAT</name>
  <what-built>Refactored Product Catalog with Search, Pagination, and Auto-Triggered Try-On.</what-built>
  <how-to-verify>
    1. Open the frontend in the browser.
    2. Upload a user portrait photo.
    3. Type "White" in the search box - observe filtered results.
    4. Click "Next" on pagination (if multiple pages exist).
    5. Click a garment - verify that virtual try-on starts automatically.
    6. Ensure result displays correctly.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
UAT of the search box, pagination, and the auto-trigger try-on flow.
</verification>

<success_criteria>
1. Product Catalog is dynamic (no hardcoded samples).
2. Search and pagination functional.
3. User can trigger try-on with a single click after photo upload.
</success_criteria>

<output>
After completion, create `.planning/phases/04-product-catalog/04-02-SUMMARY.md`
</output>
