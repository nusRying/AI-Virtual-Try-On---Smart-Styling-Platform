---
phase: 01-core-vto-engine
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [backend/main.py, tests/test_vto_pipeline.py]
autonomous: false
requirements: [VTO-01]
user_setup: []

must_haves:
  truths:
    - "FastAPI endpoint accepts image uploads and returns a generated result"
    - "End-to-end flow (upload -> preprocess -> inference -> return) completes successfully"
    - "Generated image quality meets fidelity expectations (merged, no bleeding)"
  artifacts:
    - path: "backend/main.py"
      provides: "API Gateway entry point"
    - path: "tests/test_vto_pipeline.py"
      provides: "End-to-end validation"
  key_links:
    - from: "backend/main.py"
      to: "backend/core/vton_engine.py"
      via: "core engine call"
    - from: "backend/main.py"
      to: "backend/core/preprocessor.py"
      via: "preprocessor integration"
---

<objective>
Expose the core VTO engine as a web API using FastAPI and provide an automated script for end-to-end verification. This completes the core VTO engine foundation.

Purpose: Provide an interface for future UI and platform integration.
Output: A functional FastAPI service and a complete verification suite.
</objective>

<execution_context>
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-vto-engine/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement FastAPI Try-On Service</name>
  <files>backend/main.py</files>
  <action>
    - Create `backend/main.py`.
    - Implement a FastAPI app with a single `POST /api/v1/try-on` endpoint.
    - Endpoint should accept two `UploadFile` parameters: `user_image` and `garment_image`.
    - Implement a request-level pipeline:
      1. Receive images.
      2. Call `Preprocessor` (Plan 01) for mask and pose.
      3. Call `VTONEngine` (Plan 02) for inference.
      4. Return the generated PIL image as a StreamingResponse (PNG format).
    - Handle basic validation and error reporting.
  </action>
  <verify>
    <automated>uvicorn backend.main:app --host 0.0.0.0 --port 8000 & sleep 5; curl -X POST -F "user_image=@tests/samples/user.jpg" -F "garment_image=@tests/samples/shirt.jpg" http://localhost:8000/api/v1/try-on --output output.png; pkill uvicorn</automated>
  </verify>
  <done>FastAPI service is operational and provides the virtual try-on functionality over HTTP.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Visual Pipeline Verification</name>
  <what-built>A functional local pipeline that takes two images and produces a merged try-on output.</what-built>
  <how-to-verify>
    1. Run the FastAPI server: `uvicorn backend.main:app --reload`.
    2. Open a tool like Postman or use the generated `tests/test_vto_pipeline.py` script to send images of a person and a shirt.
    3. Examine the resulting `output.png`.
    4. Confirm that:
       - The person's identity and background are preserved.
       - The garment aligns with the body pose correctly.
       - Textures and logos on the shirt are high-fidelity and clear.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Ensure that a full end-to-end request successfully completes and returns a high-resolution PNG image without errors.
</verification>

<success_criteria>
1. `backend/main.py` is fully functional and integrates pre-processing and inference.
2. The user successfully verifies the output quality through a manual/checkpoint check.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-vto-engine/01-03-SUMMARY.md`
</output>
