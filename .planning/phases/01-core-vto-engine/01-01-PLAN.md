---
phase: 01-core-vto-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/requirements.txt, backend/core/preprocessor.py, .env.example]
autonomous: true
requirements: [VTO-01]
user_setup:
  - service: HuggingFace
    why: "Access to IDM-VTON and SAM 2 weights"
    env_vars:
      - name: HF_TOKEN
        source: "HuggingFace Settings -> Access Tokens"

must_haves:
  truths:
    - "Environment supports PyTorch with CUDA (if available) and required AI libraries"
    - "MediaPipe correctly extracts 18 key pose landmarks from a human portrait"
    - "SAM 2 generates a clear binary mask separating the person/garment from the background"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "Project dependencies"
    - path: "backend/core/preprocessor.py"
      provides: "Pose and segmentation logic"
  key_links:
    - from: "backend/core/preprocessor.py"
      to: "MediaPipe"
      via: "body detection"
    - from: "backend/core/preprocessor.py"
      to: "SAM 2"
      via: "mask generation"
---

<objective>
Establish the foundation for the VTO engine by setting up the Python environment and implementing the pre-processing pipeline for pose estimation and body segmentation.

Purpose: Generate the necessary inputs (pose landmarks and segmentation masks) required by the diffusion model.
Output: A functional pre-processing module and a configured environment.
</objective>

<execution_context>
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Environment & Core Dependencies Setup</name>
  <files>backend/requirements.txt, .env.example</files>
  <action>
    - Create `backend/` directory.
    - Create `backend/requirements.txt` with: `torch`, `torchvision`, `torchaudio`, `mediapipe`, `segment-anything-2`, `opencv-python`, `pillow`, `huggingface_hub`, `fastapi`, `uvicorn`, `python-multipart`, `pydantic`, `python-dotenv`.
    - Create `.env.example` with placeholders for `HF_TOKEN` and `MODEL_CACHE_DIR`.
    - Initialize a Python virtual environment (if local) and install dependencies.
  </action>
  <verify>
    <automated>python -c "import torch, mediapipe, sam2, fastapi; print('Dependencies installed successfully')"</automated>
  </verify>
  <done>Python environment is ready with all core AI and API libraries.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Pose & Segmentation Pre-processor</name>
  <files>backend/core/preprocessor.py</files>
  <action>
    - Create `backend/core/preprocessor.py`.
    - Implement a `Preprocessor` class with:
      - `get_pose(image)`: Uses MediaPipe Pose (Landmark model) to extract 18 landmarks.
      - `get_mask(image)`: Uses SAM 2 (Segment Anything Model) to generate a binary person/garment mask from the user image.
    - Ensure output formats are compatible with IDM-VTON requirements (standardized numpy arrays or PIL images).
    - Handle edge cases where no person is detected.
  </action>
  <verify>
    <automated>python -m backend.core.preprocessor --test-image tests/samples/user.jpg</automated>
  </verify>
  <done>Preprocessor class correctly identifies body landmarks and generates segmentation masks.</done>
</task>

</tasks>

<verification>
Run a test script that takes a sample human image and outputs both the pose landmark overlay and the generated segmentation mask to a `temp/` folder.
</verification>

<success_criteria>
1. `backend/requirements.txt` includes all necessary libraries for the VTO pipeline.
2. `preprocessor.py` successfully extracts pose data and segmentation masks from sample images.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-vto-engine/01-01-SUMMARY.md`
</output>
