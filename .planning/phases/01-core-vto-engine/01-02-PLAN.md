---
phase: 01-core-vto-engine
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [backend/core/models.py, backend/core/vton_engine.py]
autonomous: true
requirements: [VTO-01]
user_setup: []

must_haves:
  truths:
    - "Model weights (IDM-VTON and SAM 2) are successfully downloaded and cached"
    - "Inference loop produces a merged image from a person and a garment image"
    - "Garment texture and logo detail are preserved in the merged output"
  artifacts:
    - path: "backend/core/models.py"
      provides: "Weight management"
    - path: "backend/core/vton_engine.py"
      provides: "IDM-VTON inference logic"
  key_links:
    - from: "backend/core/vton_engine.py"
      to: "HuggingFace"
      via: "model downloading"
    - from: "backend/core/vton_engine.py"
      to: "backend/core/preprocessor.py"
      via: "input data consumption"
---

<objective>
Implement the core virtual try-on inference engine using the IDM-VTON (SDXL-based) model. This includes managing model weights and integrating the pre-processor outputs (masks/pose) into the diffusion pipeline.

Purpose: Generate photorealistic try-on results by merging garment details onto a user's photo.
Output: A functional inference module that produces merged images.
</objective>

<execution_context>
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-vto-engine/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model Weight Management</name>
  <files>backend/core/models.py</files>
  <action>
    - Create `backend/core/models.py`.
    - Implement functions to download and load weights for:
      - IDM-VTON (SDXL and Unet components) from HuggingFace (e.g., `yisol/IDM-VTON`).
      - SAM 2 (Segment Anything Model 2) weights.
    - Use `huggingface_hub` for efficient downloading and local caching.
    - Provide a utility to verify if weights are present before inference.
  </action>
  <verify>
    <automated>python -c "from backend.core.models import verify_models; verify_models()"</automated>
  </verify>
  <done>Weights for both VTON and SAM 2 are downloaded and available for inference.</done>
</task>

<task type="auto">
  <name>Task 2: Implement VTO Inference Pipeline</name>
  <files>backend/core/vton_engine.py</files>
  <action>
    - Create `backend/core/vton_engine.py`.
    - Implement a `VTONEngine` class that:
      - Loads the IDM-VTON model components (IP-Adapter, UNet, VAE).
      - Implements `run_inference(user_image, garment_image, pose_data, person_mask)`.
      - Performs the multi-stage diffusion process to overlay the garment on the user, preserving the pose and garment details.
      - Uses cross-attention mechanisms provided by IDM-VTON to ensure realistic draping.
    - Connect the engine to the `Preprocessor` from Plan 01 for local testing.
  </action>
  <verify>
    <automated>python -m backend.core.vton_engine --test-user tests/samples/user.jpg --test-garment tests/samples/shirt.jpg</automated>
  </verify>
  <done>VTOEngine can generate merged try-on images from person and garment inputs.</done>
</task>

</tasks>

<verification>
Run the `vton_engine.py` as a script with sample user and garment images. Verify that an output image is generated in the `temp/` folder and that the garment texture is photorealistic.
</verification>

<success_criteria>
1. `models.py` correctly manages weight downloading and caching.
2. `vton_engine.py` successfully executes the diffusion pipeline and returns a merged PIL image.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-vto-engine/01-02-SUMMARY.md`
</output>
