# Plan: 08-01 Vector DB Integration (Weaviate + CLIP)

**Goal**: Establish a functional vector database for semantic search and discovery.

## Tasks
1. [x] **Install Dependencies** - `weaviate-client` and `sentence-transformers` installed.
2. [x] **Vector Logic** - Implemented `backend/core/vector_db.py` with CLIP embedding logic.
3. [ ] **Weaviate Setup** - Provide instructions or a docker-compose for a local Weaviate instance.
4. [ ] **Schema Migration** - Ensure the `Garment` class is correctly defined in Weaviate.
5. [ ] **Initial Indexing** - Run `backend/scripts/index_catalog.py` to populate the vector store.

## Success Criteria
- [ ] Weaviate is reachable and responding to health checks.
- [ ] Catalog items can be successfully embedded and stored as vectors.
- [ ] Querying by vector returns relevant garment IDs.
