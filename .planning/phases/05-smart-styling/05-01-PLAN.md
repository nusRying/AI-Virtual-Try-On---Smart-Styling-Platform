---
phase: 05-smart-styling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/core/recommendations.py, backend/core/reasoner.py, backend/main.py, backend/data/matching_items.json]
autonomous: true
requirements: [STYL-02]
---

<objective>
Implement the backend recommendation engine and AI-driven styling reasoner to provide "Complete the Look" suggestions.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Seed Matching Items Data</name>
  <files>backend/data/matching_items.json</files>
  <action>
    - Create `backend/data/matching_items.json` with sample pants and shoes.
    - Items should have metadata similar to garments but restricted to 'Pants' and 'Shoes'.
  </action>
</task>

<task type="auto">
  <name>Task 2: Implement Recommendation Service</name>
  <files>backend/core/recommendations.py</files>
  <action>
    - Create `RecommendationEngine` class.
    - Implement `get_recommendations(garment_id)` which uses tag-based similarity to find matching pants and shoes.
  </action>
</task>

<task type="auto">
  <name>Task 3: Implement AI Reasoner</name>
  <files>backend/core/reasoner.py</files>
  <action>
    - Create `StylingReasoner` class.
    - Implement `generate_styling_tip(selected_item, matched_items)` that returns a brief AI-style justification.
  </action>
</task>

<task type="auto">
  <name>Task 4: Expose Recommendation Endpoint</name>
  <files>backend/main.py</files>
  <action>
    - Add `GET /api/v1/recommendations/{garment_id}`.
    - Return matching items and the styling tip.
  </action>
</task>
</tasks>
