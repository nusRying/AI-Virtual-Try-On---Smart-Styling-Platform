---
phase: 02-async-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [tests/test_vto_pipeline.py]
autonomous: true
requirements: [PLAT-01]

must_haves:
  truths:
    - "Full async flow is verified from upload to result fetch"
    - "Test script correctly polls the task status"
    - "Worker successfully processes the inference task"
  artifacts:
    - path: "tests/test_vto_pipeline.py"
      provides: "Async-ready E2E test client"
  key_links:
    - from: "tests/test_vto_pipeline.py"
      to: "/api/v1/tasks"
      via: "HTTP GET"
      pattern: "requests\\.get.*tasks"
---

<objective>
Verify the end-to-end asynchronous virtual try-on workflow using the updated API and worker infrastructure.

Purpose: Ensure the system successfully processes requests across multiple services (API -> Redis -> Worker -> Disk).
Output: Verified async pipeline and updated test suite.
</objective>

<execution_context>
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/test_vto_pipeline.py
@backend/main.py
@backend/tasks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update E2E test script for async support</name>
  <files>tests/test_vto_pipeline.py</files>
  <action>
    - Refactor `tests/test_vto_pipeline.py` to:
      1. Submit the initial `POST /api/v1/try-on` request.
      2. Capture the returned `task_id`.
      3. Enter a loop that polls `GET /api/v1/tasks/{task_id}` every 2 seconds.
      4. Break on `SUCCESS` or `FAILURE` status.
      5. On `SUCCESS`, download the final result image from the `result_url`.
  </action>
  <verify>
    <automated>grep "task_id" tests/test_vto_pipeline.py</automated>
  </verify>
  <done>Test script is ready for async E2E verification.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Async E2E Verification</what-built>
  <how-to-verify>
    - Ensure Redis is running.
    - Start the FastAPI server: `uvicorn backend.main:app --reload`.
    - Start the Celery worker in a separate terminal: `celery -A backend.celery_app worker --loglevel=info -P solo`.
    - Run the E2E test: `python tests/test_vto_pipeline.py --test-user <path> --test-garment <path>`.
    - Observe the logs:
      1. API receives upload and returns task_id.
      2. Worker receives task and runs inference (with mock).
      3. API polling eventually returns SUCCESS.
      4. Test script downloads the output.
  </how-to-verify>
  <resume-signal>E2E workflow verified successfully.</resume-signal>
</task>

</tasks>

<verification>
Check for output image creation:
`ls backend/results/` should contain the generated PNG.
</verification>

<success_criteria>
- The test script completes the full cycle (submit -> poll -> download).
- The Celery worker logs show task execution without errors.
- The downloaded image is a valid PNG.
</success_criteria>

<output>
After completion, create `.planning/phases/02-async-infrastructure/02-03-SUMMARY.md`
</output>
