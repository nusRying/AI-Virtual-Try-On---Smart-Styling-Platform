---
phase: 02-async-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/requirements.txt, .env.example, backend/celery_app.py, backend/tasks.py]
autonomous: true
requirements: [PLAT-01]
user_setup:
  - service: redis
    why: "Message broker for Celery"
    env_vars:
      - name: REDIS_URL
        source: "Set to 'redis://localhost:6379/0' for local installation"
    dashboard_config:
      - task: "Ensure Redis server is running"
        location: "Local machine (e.g., redis-server) or Docker container"

must_haves:
  truths:
    - "Celery app is configured to use Redis as broker and backend"
    - "VTO pipeline is encapsulated in a Celery task"
    - "Celery worker can be started without import errors"
  artifacts:
    - path: "backend/celery_app.py"
      provides: "Celery instance configuration"
    - path: "backend/tasks.py"
      provides: "vto_task implementation"
  key_links:
    - from: "backend/tasks.py"
      to: "backend/core/vton_engine.py"
      via: "import VTONEngine"
      pattern: "from backend\\.core\\.vton_engine import VTONEngine"
---

<objective>
Establish the asynchronous task infrastructure using Celery and Redis to enable non-blocking inference.

Purpose: Offload GPU-heavy VTO operations to a worker process.
Output: Configured Celery application and task definitions.
</objective>

<execution_context>
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/requirements.txt
@backend/core/vton_engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dependencies and environment</name>
  <files>backend/requirements.txt, .env.example</files>
  <action>
    - Add `celery` and `redis` to `backend/requirements.txt`.
    - Add `REDIS_URL=redis://localhost:6379/0` and `CELERY_WORKER_CONCURRENCY=1` to `.env.example`.
  </action>
  <verify>
    <automated>grep -E "celery|redis" backend/requirements.txt</automated>
  </verify>
  <done>Dependencies and environment variables are defined.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Celery application</name>
  <files>backend/celery_app.py</files>
  <action>
    - Create `backend/celery_app.py`.
    - Initialize a Celery instance using `REDIS_URL` from environment variables.
    - Set up standard Celery configurations (broker, backend, result_expires).
    - Ensure it loads tasks from `backend.tasks`.
  </action>
  <verify>
    <automated>python -c "from backend.celery_app import celery_app; print(celery_app.main)"</automated>
  </verify>
  <done>Celery application is initialized and points to the correct broker.</done>
</task>

<task type="auto">
  <name>Task 3: Implement VTO Celery task</name>
  <files>backend/tasks.py</files>
  <action>
    - Create `backend/tasks.py`.
    - Define a `vto_task` decorated with `@celery_app.task`.
    - The task should:
      1. Accept `user_image_path`, `garment_image_path`, and `output_path`.
      2. Lazily initialize `Preprocessor` and `VTONEngine` (load once per worker process).
      3. Execute the full VTO pipeline (Pre-process -> Inference -> Save).
      4. Handle errors and return a success status with the output path.
  </action>
  <verify>
    <automated>python -c "from backend.tasks import vto_task; print(vto_task.name)"</automated>
  </verify>
  <done>VTO pipeline is successfully wrapped in a Celery task.</done>
</task>

</tasks>

<verification>
Check if celery worker can be initialized:
`celery -A backend.celery_app inspect ping` (requires Redis running)
</verification>

<success_criteria>
- Celery app successfully imports and recognizes the `vto_task`.
- `backend/tasks.py` correctly imports and utilizes the Phase 1 components.
</success_criteria>

<output>
After completion, create `.planning/phases/02-async-infrastructure/02-01-SUMMARY.md`
</output>
