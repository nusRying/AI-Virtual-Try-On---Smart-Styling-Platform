---
phase: 02-async-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [backend/main.py]
autonomous: false
requirements: [PLAT-01]

must_haves:
  truths:
    - "API returns a JSON object with task_id immediately after upload"
    - "Status endpoint returns the current state of a task (PENDING, SUCCESS, etc.)"
    - "Successful tasks provide a URL to the final image"
    - "Final images are accessible via a static route"
  artifacts:
    - path: "backend/main.py"
      provides: "Updated FastAPI endpoints"
  key_links:
    - from: "backend/main.py"
      to: "backend/tasks.py"
      via: "task submission"
      pattern: "vto_task\\.delay"
---

<objective>
Refactor the FastAPI application to orchestrate the asynchronous VTO workflow.

Purpose: Provide a responsive API that decouples client requests from long-running inference tasks.
Output: Updated API with non-blocking try-on submission and polling.
</objective>

<execution_context>
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/main.py
@backend/tasks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor try-on submission and implement polling</name>
  <files>backend/main.py</files>
  <action>
    - Update `POST /api/v1/try-on`:
      1. Create `backend/uploads/` and `backend/results/` directories if they don't exist.
      2. Save uploaded `user_image` and `garment_image` to `backend/uploads/` with unique names.
      3. Define `output_path` in `backend/results/`.
      4. Trigger `vto_task.delay(user_path, garment_path, output_path)`.
      5. Return `{"task_id": result.id, "status": "PENDING"}`.
    - Implement `GET /api/v1/tasks/{task_id}`:
      1. Retrieve task results using `AsyncResult(task_id)`.
      2. Return status, and if `SUCCESS`, include a `result_url`.
    - Use `FastAPI`'s `StaticFiles` to serve `backend/results/` at `/results`.
  </action>
  <verify>
    <automated>python -c "import backend.main; print(backend.main.app.routes)"</automated>
  </verify>
  <done>API is updated with async endpoints and static file serving.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Async API Endpoints</what-built>
  <how-to-verify>
    - Start the FastAPI server: `uvicorn backend.main:app --reload`.
    - Check the OpenAPI docs at `http://localhost:8000/docs`.
    - Verify that `/api/v1/try-on` and `/api/v1/tasks/{task_id}` are listed.
  </how-to-verify>
  <resume-signal>Endpoints confirmed.</resume-signal>
</task>

</tasks>

<verification>
Check for static file mounting:
`curl -I http://localhost:8000/results/test.png` should return a response (even 404 is fine as long as it's handled by StaticFiles).
</verification>

<success_criteria>
- Submission returns a 200/202 status with a `task_id`.
- Polling returns valid Celery task states.
- Static file serving is active.
</success_criteria>

<output>
After completion, create `.planning/phases/02-async-infrastructure/02-02-SUMMARY.md`
</output>
