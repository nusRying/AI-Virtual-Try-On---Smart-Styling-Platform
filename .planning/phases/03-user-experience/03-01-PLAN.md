---
phase: 03-user-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["frontend/package.json", "frontend/src/components/UserPhotoUpload.tsx", "frontend/src/components/UserPhotoUpload.css", "frontend/vite.config.ts"]
autonomous: true
requirements: ["VTO-02"]
must_haves:
  truths:
    - "User can see a file upload zone with clear instructions"
    - "Invalid file uploads (too large, wrong format, landscape) are rejected immediately"
    - "User can see a preview of their uploaded portrait photo"
  artifacts:
    - path: "frontend/package.json"
      provides: "React/Vite project foundation"
    - path: "frontend/src/components/UserPhotoUpload.tsx"
      provides: "Validation logic for size and aspect ratio"
      contains: "VTO-02"
  key_links:
    - from: "frontend/src/components/UserPhotoUpload.tsx"
      to: "window.URL.createObjectURL"
      via: "previewing the image locally"
---

<objective>
Setup the React (Vite) frontend environment and implement the core photo upload component with real-time validation logic.

Purpose: To ensure user photos meet the system's quality and format requirements (portrait, size) before submission.
Output: A functional React project with a validated upload form.
</objective>

<execution_context>
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Frontend Project</name>
  <files>frontend/package.json, frontend/vite.config.ts</files>
  <action>
    - Initialize a new Vite project in the `frontend/` directory using the `react-ts` template.
    - Install necessary dependencies: `axios`, `lucide-react`.
    - Configure `frontend/vite.config.ts` to run on port 5173 and set up an alias for `src` if needed.
    - Clean up default Vite boilerplate (App.css, index.css) to prepare for Vanilla CSS.
  </action>
  <verify>
    <automated>cd frontend && npm list react axios lucide-react</automated>
  </verify>
  <done>Vite project structure exists and dependencies are installed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement UserPhotoUpload with Validation</name>
  <files>frontend/src/components/UserPhotoUpload.tsx, frontend/src/components/UserPhotoUpload.css</files>
  <action>
    - Create a `UserPhotoUpload` component that handles file selection (drag-and-drop or click).
    - Implement real-time validation (per VTO-02):
      - File size: Max 5MB.
      - Format: JPG, PNG.
      - Aspect Ratio: Must be portrait (height > width). Use `Image` and `onload` to check dimensions.
    - Display error messages for each validation failure.
    - Show a local preview of the photo once valid.
    - Use Vanilla CSS for all styling (no external CSS frameworks).
  </action>
  <verify>
    <automated>ls frontend/src/components/UserPhotoUpload.tsx</automated>
  </verify>
  <done>User can select a file, see validation errors, and see a valid portrait preview.</done>
</task>

</tasks>

<verification>
Check if `frontend/package.json` contains React and Vite dependencies.
Check if `UserPhotoUpload.tsx` contains the aspect ratio validation logic (`img.height > img.width`).
</verification>

<success_criteria>
- Frontend project is initialized and runs.
- Upload component rejects landscape images and files > 5MB.
- Upload component displays a preview of a valid portrait photo.
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-experience/03-01-SUMMARY.md`
</output>
