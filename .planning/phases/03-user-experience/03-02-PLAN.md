---
phase: 03-user-experience
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: ["backend/main.py", "frontend/src/components/GarmentSelection.tsx", "frontend/src/services/api.ts", "frontend/src/App.tsx", "frontend/src/App.css"]
autonomous: false
requirements: ["VTO-02"]
must_haves:
  truths:
    - "CORS preflight requests from frontend are handled by the backend"
    - "User can select a garment from a visible list"
    - "The UI shows a 'Generating...' state while polling the backend"
    - "The final try-on image is rendered in the browser"
  artifacts:
    - path: "backend/main.py"
      provides: "CORS configuration"
      contains: "CORSMiddleware"
    - path: "frontend/src/services/api.ts"
      provides: "Axios integration and polling logic"
      exports: ["submitTryOnTask", "pollTaskStatus"]
    - path: "frontend/src/components/GarmentSelection.tsx"
      provides: "Visual selection of target items"
  key_links:
    - from: "frontend/src/services/api.ts"
      to: "http://localhost:8000/api/v1/try-on"
      via: "POST request with FormData"
    - from: "frontend/src/services/api.ts"
      to: "http://localhost:8000/api/v1/tasks/{id}"
      via: "Polling GET request every 2s"
---

<objective>
Enable secure cross-origin communication between frontend and backend, and implement the end-to-end try-on workflow (submission, polling, and result display).

Purpose: To bridge the gap between user interaction and the asynchronous AI processing pipeline.
Output: A fully functional web interface that triggers and displays virtual try-ons.
</objective>

<execution_context>
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/umair/Videos/AI Virtual Try-On + Smart Styling Platform/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@backend/main.py
@frontend/src/components/UserPhotoUpload.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable CORS in FastAPI Backend</name>
  <files>backend/main.py</files>
  <action>
    - Import `CORSMiddleware` from `fastapi.middleware.cors`.
    - Add the middleware to the FastAPI app.
    - Configure it to allow `http://localhost:5173` (Vite default) and all methods/headers.
  </action>
  <verify>
    <automated>python -c "from backend.main import app; print('CORSMiddleware' in [m.cls.__name__ for m in app.user_middleware])"</automated>
  </verify>
  <done>Backend allows requests from the frontend origin.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Garment Selection and API Service</name>
  <files>frontend/src/components/GarmentSelection.tsx, frontend/src/services/api.ts</files>
  <action>
    - Create `frontend/src/services/api.ts` using Axios.
    - Implement `submitTryOnTask(userImage: File, garmentImage: File)`: POST to `/api/v1/try-on`.
    - Implement `pollTaskStatus(taskId: string)`: GET to `/api/v1/tasks/{id}` with a delay/retry loop.
    - Create `GarmentSelection` component providing a few sample garments (using static images).
    - When a garment is selected, convert the image to a File/Blob (if using local assets) to prepare for submission.
  </action>
  <verify>
    <automated>ls frontend/src/services/api.ts</automated>
  </verify>
  <done>API service and selection component are ready for integration.</done>
</task>

<task type="auto">
  <name>Task 3: Build Try-On Workflow & Result Display</name>
  <files>frontend/src/App.tsx, frontend/src/App.css</files>
  <action>
    - Integrate `UserPhotoUpload` and `GarmentSelection` in `App.tsx`.
    - Manage application state: `selectedUserPhoto`, `selectedGarment`, `taskId`, `isProcessing`, `resultUrl`, `error`.
    - Implement a "Generate Try-On" button that:
      - Validates both images are selected.
      - Calls the API service to submit the task.
      - Enters polling state, showing a spinner/loading bar.
      - Upon success, displays the result image from `/api/v1/results/{filename}`.
    - Style the layout using Vanilla CSS (2-column layout for inputs, large result area below).
  </action>
  <verify>
    <automated>ls frontend/src/App.tsx</automated>
  </verify>
  <done>End-to-end frontend workflow is implemented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Virtual Try-On Web Interface</what-built>
  <how-to-verify>
    1. Start Backend: `uvicorn backend.main:app --reload`
    2. Start Celery: `celery -A backend.tasks worker --loglevel=info` (ensure Redis is running)
    3. Start Frontend: `cd frontend && npm run dev`
    4. Open Browser at `http://localhost:5173`.
    5. Upload a portrait photo (JPG).
    6. Select a garment.
    7. Click 'Generate Try-On'.
    8. Verify 'Generating...' state appears.
    9. Verify final image appears in the result area after a few seconds.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Verify `backend/main.py` has CORS headers.
Verify `App.tsx` handles the transition from 'idle' to 'processing' to 'success/error'.
</verification>

<success_criteria>
- User can trigger a try-on from the browser.
- Polling correctly waits for the async task to complete.
- The resulting image is displayed correctly in the browser.
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-experience/03-02-SUMMARY.md`
</output>
